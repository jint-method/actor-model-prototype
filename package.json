{
  "name": "test",
  "version": "0.1.0",
  "description": "Testing a variation of the Actor Model of web development.",
  "author": "Kyle Andrews",
  "license": "MIT",
  "scripts": {
    "compile": "tsc",
    "preview": "serve -d ./docs",
    "cleanup": "rm -rf ./docs/assets",
    "build": "npm run cleanup && npm run compile && npm run preview"
  },
  "dependencies": {
    "typescript": "^3.7.2",
    "serve": "^11.2.0"
  }
}
